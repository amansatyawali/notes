from utils import is_sorted, create_random_int_array, less

def partition(arr, first , last ) :



if __name__ == "__main__" :
    
    arr = [4, 3]
    print(arr)
    partition(arr, first = 0, mid = 1, last = 1)
    print(arr)
    print(is_sorted(arr, len(arr)))